class fsrm_fsrmexportimport_1 {
    constructor() {
    }

    // void ExportFileGroups (string, Variant, string)
    ExportFileGroups(string, Variant, string) {

    }

    // void ExportFileScreenTemplates (string, Variant, string)
    ExportFileScreenTemplates(string, Variant, string) {

    }

    // void ExportQuotaTemplates (string, Variant, string)
    ExportQuotaTemplates(string, Variant, string) {

    }

    // IFsrmCommittableCollection ImportFileGroups (string, Variant, string)
    ImportFileGroups(string, Variant, string) {

    }

    // IFsrmCommittableCollection ImportFileScreenTemplates (string, Variant, string)
    ImportFileScreenTemplates(string, Variant, string) {

    }

    // IFsrmCommittableCollection ImportQuotaTemplates (string, Variant, string)
    ImportQuotaTemplates(string, Variant, string) {

    }

}

module.exports = fsrm_fsrmexportimport_1;

