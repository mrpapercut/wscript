class msvidctl_msvidanalogtunerdevice {
    constructor() {
        // int AudioFrequency () {get}
        this.AudioFrequency = undefined;

        // string Category () {get}
        this.Category = undefined;

        // int Channel () {get} {set}
        this.Channel = undefined;

        // string ClassID () {get}
        this.ClassID = undefined;

        // int CountryCode () {get} {set}
        this.CountryCode = undefined;

        // string Name () {get}
        this.Name = undefined;

        // int NumAuxInputs () {get}
        this.NumAuxInputs = undefined;

        // bool Power () {get} {set}
        this.Power = undefined;

        // bool SAP () {get} {set}
        this.SAP = undefined;

        // int Status () {get}
        this.Status = undefined;

        // ITuneRequest Tune () {get} {set}
        this.Tune = undefined;

        // int TunerModes () {get}
        this.TunerModes = undefined;

        // ITuningSpace TuningSpace () {get}
        this.TuningSpace = undefined;

        // int TVFormats () {get}
        this.TVFormats = undefined;

        // int VideoFrequency () {get}
        this.VideoFrequency = undefined;

    }

    // bool ChannelAvailable (int, int)
    ChannelAvailable(int, int) {

    }

    // bool IsEqualDevice (IMSVidDevice)
    IsEqualDevice(IMSVidDevice) {

    }

    // bool IsViewable (Variant)
    IsViewable(Variant) {

    }

    // void View (Variant)
    View(Variant) {

    }

}

module.exports = msvidctl_msvidanalogtunerdevice;

