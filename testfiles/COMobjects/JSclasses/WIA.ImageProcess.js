class wia_imageprocess {
    constructor() {
        // IFilterInfos FilterInfos () {get}
        this.FilterInfos = undefined;

        // IFilters Filters () {get}
        this.Filters = undefined;

    }

    // IImageFile Apply (IImageFile)
    Apply(IImageFile) {

    }

}

module.exports = wia_imageprocess;

