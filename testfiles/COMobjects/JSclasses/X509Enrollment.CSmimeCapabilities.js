class x509enrollment_csmimecapabilities {
    constructor() {
        // ISmimeCapability ItemByIndex (int) {get}
        this.Parameterized = undefined;

        // int Count () {get}
        this.Count = undefined;

    }

    // void Add (ISmimeCapability)
    Add(ISmimeCapability) {

    }

    // void AddAvailableSmimeCapabilities (bool)
    AddAvailableSmimeCapabilities(bool) {

    }

    // void AddFromCsp (ICspInformation)
    AddFromCsp(ICspInformation) {

    }

    // void Clear ()
    Clear() {

    }

    // void Remove (int)
    Remove(int) {

    }

}

module.exports = x509enrollment_csmimecapabilities;

