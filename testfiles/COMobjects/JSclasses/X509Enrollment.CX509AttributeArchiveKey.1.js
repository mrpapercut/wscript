class x509enrollment_cx509attributearchivekey_1 {
    constructor() {
        // string RawData (EncodingType) {get}
        this.Parameterized = undefined;

        // IObjectId EncryptionAlgorithm () {get}
        this.EncryptionAlgorithm = undefined;

        // int EncryptionStrength () {get}
        this.EncryptionStrength = undefined;

        // IObjectId ObjectId () {get}
        this.ObjectId = undefined;

    }

    // void Initialize (IObjectId, EncodingType, string)
    Initialize(IObjectId, EncodingType, string) {

    }

    // void InitializeDecode (EncodingType, string)
    InitializeDecode(EncodingType, string) {

    }

    // void InitializeEncode (IX509PrivateKey, EncodingType, string, IObjectId, int)
    InitializeEncode(IX509PrivateKey, EncodingType, string, IObjectId, int) {

    }

}

module.exports = x509enrollment_cx509attributearchivekey_1;

