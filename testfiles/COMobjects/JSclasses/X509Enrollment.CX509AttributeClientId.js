class x509enrollment_cx509attributeclientid {
    constructor() {
        // string RawData (EncodingType) {get}
        this.Parameterized = undefined;

        // RequestClientInfoClientId ClientId () {get}
        this.ClientId = undefined;

        // string MachineDnsName () {get}
        this.MachineDnsName = undefined;

        // IObjectId ObjectId () {get}
        this.ObjectId = undefined;

        // string ProcessName () {get}
        this.ProcessName = undefined;

        // string UserSamName () {get}
        this.UserSamName = undefined;

    }

    // void Initialize (IObjectId, EncodingType, string)
    Initialize(IObjectId, EncodingType, string) {

    }

    // void InitializeDecode (EncodingType, string)
    InitializeDecode(EncodingType, string) {

    }

    // void InitializeEncode (RequestClientInfoClientId, string, string, string)
    InitializeEncode(RequestClientInfoClientId, string, string, string) {

    }

}

module.exports = x509enrollment_cx509attributeclientid;

