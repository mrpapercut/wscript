class x509enrollment_cx509attributecspprovider {
    constructor() {
        // string Signature (EncodingType) {get}
        this.Parameterized = undefined;

        // X509KeySpec KeySpec () {get}
        this.KeySpec = undefined;

        // IObjectId ObjectId () {get}
        this.ObjectId = undefined;

        // string ProviderName () {get}
        this.ProviderName = undefined;

    }

    // void Initialize (IObjectId, EncodingType, string)
    Initialize(IObjectId, EncodingType, string) {

    }

    // void InitializeDecode (EncodingType, string)
    InitializeDecode(EncodingType, string) {

    }

    // void InitializeEncode (X509KeySpec, string, EncodingType, string)
    InitializeEncode(X509KeySpec, string, EncodingType, string) {

    }

}

module.exports = x509enrollment_cx509attributecspprovider;

