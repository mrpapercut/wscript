class x509enrollment_cx509certificatetemplateadwritable {
    constructor() {
        // Variant Property (EnrollmentTemplateProperty) {get} {set}
        this.Parameterized = undefined;

        // IX509CertificateTemplate Template () {get}
        this.Template = undefined;

    }

    // void Commit (CommitTemplateFlags, string)
    Commit(CommitTemplateFlags, string) {

    }

    // void Initialize (IX509CertificateTemplate)
    Initialize(IX509CertificateTemplate) {

    }

}

module.exports = x509enrollment_cx509certificatetemplateadwritable;

