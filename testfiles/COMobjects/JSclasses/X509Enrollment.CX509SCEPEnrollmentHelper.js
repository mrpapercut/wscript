class x509enrollment_cx509scepenrollmenthelper {
    constructor() {
        // string ResultMessageText () {get}
        this.ResultMessageText = undefined;

        // IX509SCEPEnrollment X509SCEPEnrollment () {get}
        this.X509SCEPEnrollment = undefined;

    }

    // X509SCEPDisposition Enroll (X509SCEPProcessMessageFlags)
    Enroll(X509SCEPProcessMessageFlags) {

    }

    // X509SCEPDisposition FetchPending (X509SCEPProcessMessageFlags)
    FetchPending(X509SCEPProcessMessageFlags) {

    }

    // void Initialize (string, string, IX509CertificateRequestPkcs10, string)
    Initialize(string, string, IX509CertificateRequestPkcs10, string) {

    }

    // void InitializeForPending (string, string, X509CertificateEnrollmentContext, string)
    InitializeForPending(string, string, X509CertificateEnrollmentContext, string) {

    }

}

module.exports = x509enrollment_cx509scepenrollmenthelper;

